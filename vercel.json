{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd www && npm install && npm run build",
  "outputDirectory": "www/dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}